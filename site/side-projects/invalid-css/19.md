---
layout: default
title: ":flex-nth-in-row"
date: 2026-08-23 12:00:00
tags:
  - invalid-css
description: "“Invalid CSS” is a project that features CSS declarations that are not valid and non-existing. For example, :flex-nth-in-row."
type: articles-item
---

```css
:flex-nth-in-row
```

Sometimes you would like to highlight the flex item in wrapped flex container but it doesn't work when there are multiple rows. `:flex-nth-in-row` would make it easier to target desired nth child in row when flex container starts wrapping.

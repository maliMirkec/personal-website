{% assign mySlides = collections['mySlides'] | reverse %}
<section>
  <ul>
    {% for slide in mySlides %}
      <li><a href="{{ slide.url }}">{{ slide.data.title }} ⇢</a></li>
    {% endfor %}
  </ul>
</section>

{% if sections %}
  <hr class="text-alpha hr">
{% endif %}
{% if tags and tags[0] == 'blog' %}
  {% include _partials/hero-gamma %}
  <hr class="text-alpha hr">
{% endif %}
<div class="wrapper margin-top-alpha margin-bottom-alpha">
  <div class="padding">
    {% if tags and tags[0] == 'blog' %}
      <div class="copy copy--body margin-bottom">
        <p class="fs-italic">This article was originally published at <a href="{{ canonical }}">{{ publication }}.</a></p>
      </div>
    {% endif %}
    <div class="copy copy--body">{{ content }}</div>
  </div>
</div>
{% if tags and tags[0] == 'blog' %}
  <hr class="text-alpha hr">
  {% include _partials/banner-gamma %}
{% endif %}
{% if sections2 %}
  <hr class="text-alpha hr">
{% endif %}
